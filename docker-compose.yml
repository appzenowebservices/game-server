services:
  patangbaz:
    build: .
    image: patangbaz-api:latest
    container_name: patangbaz
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      HOST: "0.0.0.0"
      PORT: "8787"
      PUBLIC_URL: "https://www.game-server.appzenowebservices.com"
    networks:
      - web
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8787/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

networks:
  web:
    external: true